<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="common_dialog" parent="@android:style/Theme.Dialog" type="text/css">
        <item name="android:windowIsFloating">true</item>
        <!-- 设置未浮动窗口 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置无边框 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 设置完全透明 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 设置屏幕变暗 -->
    </style>

    <!-- splash页面主题 -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <!-- 将splash图片设置在这，这样这张图片取代白屏 -->
        <!--<item name="android:windowBackground">@drawable/guide_new</item>-->
        <item name="android:windowFullscreen">true</item>
        <!-- 或者是将主题设置成透明，虽然点击图标会有点小小的卡顿，但是用户体验不影响 -->
        <item name="android:windowIsTranslucent">true</item> <!-- 透明背景不要了 -->
    </style>

    <!-- 统计中心页面主题 -->
    <style name="CountCenterTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="vpiTabPageIndicatorStyle">@style/MyWidget.TabPageIndicator</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:animationDuration">5000</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="MyWidget.TabPageIndicator" parent="Widget">
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/vpi__tab_indicator</item>
        <item name="android:paddingLeft">22dip</item>
        <item name="android:paddingRight">22dip</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textAppearance">@style/MyTextAppearance.TabPageIndicator</item>
        <item name="android:textSize">14sp</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="MyTextAppearance.TabPageIndicator" parent="Widget">
        <!-- 设置文本字体加粗 -->
        <!--<item name="android:textStyle">bold</item>-->
        <!-- 设置文本字体颜色选择器 -->
        <item name="android:textColor">@drawable/indicator_text_color</item>
    </style>

    <!-- CollapsedTitleTheme -->
    <style name="CollapsedTitleTheme">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!--&lt;!&ndash; 添加物品页面主题 &ndash;&gt;-->
    <!--<style name="AddObjectTheme" parent="Theme.AppCompat.Light.NoActionBar">-->
        <!--<item name="android:windowAnimationStyle">@style/AnimationActivity</item>-->
    <!--</style>-->
    <!--&lt;!&ndash; 添加物品界面动画 &ndash;&gt;-->
    <!--<style name="AnimationActivity" parent="@android:style/Animation.Activity">-->
        <!--<item name="android:activityOpenEnterAnimation">@anim/open_enter</item>-->
        <!--<item name="android:activityOpenExitAnimation">@anim/open_exit</item>-->
        <!--<item name="android:activityCloseEnterAnimation">@anim/close_enter</item>-->
        <!--<item name="android:activityCloseExitAnimation">@anim/close_exit</item>-->
    <!--</style>-->

</resources>
