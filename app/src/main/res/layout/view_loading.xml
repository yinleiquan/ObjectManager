<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/transparent"
    android:clickable="true">
    <LinearLayout
        android:layout_width="@dimen/dip50"
        android:layout_height="@dimen/dip50"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:background="@drawable/loading_dialog_bg">
        <ImageView
            android:id="@+id/loading_logo_progress"
            android:layout_width="@dimen/dip30"
            android:layout_height="@dimen/dip30"
            android:background="@drawable/loading_logo_progress" />
    </LinearLayout>
</RelativeLayout>